<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TicketManager.View.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TicketManager.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <TextBlock Text="Welcome to WizzErr" FontSize="32" FontWeight="Bold" Foreground="#2bb8c0" HorizontalAlignment="Center"/>
            <TextBlock Text="Please sign in to manage your tickets" FontSize="16" Foreground="Gray" HorizontalAlignment="Center"/>

            <TextBox PlaceholderText="Username or Email" Width="300"/>
            <PasswordBox PlaceholderText="Password" Width="300"/>

            <Button Content="Sign In" 
                    Click="SignInButton_Click"
                    Background="#2bb8c0" Foreground="White" 
                    Width="300" Height="40" CornerRadius="8" 
                    FontWeight="Bold" Margin="0,10,0,0"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="5" Margin="0,10,0,0">
                <TextBlock Text="Don't have an account?" VerticalAlignment="Center" Foreground="Gray"/>
                <HyperlinkButton Content="Create one" Click="CreateAccount_Click" Foreground="#2bb8c0" FontWeight="SemiBold"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
