<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TicketManager.View.AuthPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TicketManager.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Transparent">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <TextBlock x:Name="TitleTextBlock" Text="Welcome to WizzErr" FontSize="32" FontWeight="Bold" Foreground="#2bb8c0" HorizontalAlignment="Center"/>
            <TextBlock x:Name="SubtitleTextBlock" Text="Please sign in to manage your tickets" FontSize="16" Foreground="Gray" HorizontalAlignment="Center"/>

            <TextBox x:Name="emailInput" PlaceholderText="Email" Width="300" Visibility="Visible" InputScope="EmailSmtpAddress" TextChanged="Input_TextChanged" />
            <TextBox x:Name="usernameInput" PlaceholderText="Username" Width="300" Visibility="Collapsed" TextChanged="Input_TextChanged" />
            <TextBox x:Name="phoneInput" PlaceholderText="Phone Number" Width="300" Visibility="Collapsed" InputScope="TelephoneNumber" TextChanged="Input_TextChanged" />
            <PasswordBox x:Name="passwordInput" PlaceholderText="Password" Width="300" Visibility="Visible" PasswordChanged="Password_PasswordChanged" />

            <Button x:Name="loginButton" Content="Sign In" 
                    Click="ActionButton_Click"
                    Background="#2bb8c0" Foreground="White" 
                    Width="300" Height="40" CornerRadius="8" 
                    FontWeight="Bold" Margin="0,10,0,0"
                    HorizontalAlignment="Center" HorizontalContentAlignment="Center"
                    IsEnabled="False"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="5" Margin="0,10,0,0">
                <TextBlock x:Name="TogglePromptText" Text="Don't have an account?" VerticalAlignment="Center" Foreground="Gray"/>
                <HyperlinkButton x:Name="ToggleModeButton" Content="Create one" Click="ToggleMode_Click" Foreground="#2bb8c0" FontWeight="SemiBold"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
